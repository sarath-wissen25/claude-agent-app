from __future__ import annotations


if __name__ == "__main__":
    raise SystemExit(
        "This project now targets Databricks Apps + MLflow Agent Server. "
        "Use `python -m app.main` and deploy with `app.yaml` instead of Model Serving registration."
    )
